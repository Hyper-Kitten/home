!!!
%html.h-100
  %head
    %title= @page&.title || t('title')
    %meta{"http-equiv" => "x-ua-compatible", content: "ie=edge"}
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0, maximum-scale=1.0"}
    %meta{charset: "utf-8"}
    %meta{name: "description", content: (content_for(:page_description) || t('page_description'))}
    %meta{name: "robots", content: "index,follow,noodp"}
    %meta{name: "generator", content: "HyperKitten - Meow"}
    %meta{name: "subject", content: t('site_subject')}
    %base{href: request.base_url}
    = stylesheet_link_tag "application"
    = csrf_meta_tags
  %body.d-flex.flex-column.h-100
    %header.bg-light.py-3.mb-3
      %nav.navbar.navbar-expand-lg.navbar-light
        .container.d-flex.justify-content-between
          %h1.navbar-brand= link_to t('title'), '/'
          %ul.navbar-nav.mb-2.mb-lg-0
            - main_menu = menu("main-menu")
            - if main_menu.present?
              - main_menu.menu_items.each do |menu_item|
                %li.nav-item= link_to menu_item.title, (menu_item.page || menu_item.url), target: menu_item.target, class: "nav-link"
    = yield
    %footer.footer.mt-auto.py-3.bg-light
      .container
        %p= "By #{t('owner')}"
